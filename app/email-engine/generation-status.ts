// Global generation hold. This module is intentionally dependency-free so
// both the server and the editor UI can read the same status without
// bundling the Claude SDK.
export const EMAIL_GENERATION_PAUSED = false;

Nomi's button — use for any action in the admin UI; it sets in the system sans so it sits with Polaris.

```jsx
<Button variant="primary" onClick={send}>Send now</Button>
<Button variant="ghost">Preview</Button>
```

One primary per view. Secondary is outlined, ghost is bare text in the darker cyan (#00789e) so it clears contrast at body size.
